/**
 * Alias histórico alineado al rol (AD-11).
 * Preferir `Roles` / `user.rol` en código nuevo.
 */
export enum TipoUsuario {
  OPERATIVO = 'operativo',
  ADMIN_TENANT = 'admin_tenant',
  ADMIN_SISTEMA = 'admin_sistema',
}
