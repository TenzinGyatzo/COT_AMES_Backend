/** Discriminador catálogo unificado (AD-19 / FR-58 / Story 6.1). */
export enum TipoItem {
  SERVICIO = 'servicio',
  PRODUCTO = 'producto',
}

export const TIPO_ITEM_VALUES = Object.values(TipoItem);
